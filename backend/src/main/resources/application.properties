# Spring Profile Configuration
# Default to 'dev' profile for local development
# Production (Render) will override with 'prod' profile
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application Configuration
spring.application.name=Tujulishane Hub Backend
server.port=${PORT:8080}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:QwErTyUiOpAsDfGhJkLzXcVbNmQwErTyUiOpAsDfGhJkLzXcVbNmQwErTyUiOpAsDfGhJkLzXcVbNm}
jwt.expiration=3600

# Mail Configuration (for OTP emails)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Geocoding Configuration
geocoding.enabled=true
geocoding.api.key=${GEOCODING_API_KEY:}

# Application Configuration
spring.application.name=Tujulishane Hub Backend
server.port=${PORT:8080}

# Logging Configuration
logging.level.com.tujulishanehub.backend=DEBUG
logging.level.org.springframework.web=DEBUG

# JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Africa/Nairobi